export function saveData(saveData = {}) {
    // Add to the save data what you want to be stored
    // These should be things not in the state
    return saveData
}

export function loadData(saveData) {
    // Hydrate objects from save data
}